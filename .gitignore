# Build tooling / artifacts (not deployed — see .github/workflows/deploy.yml excludes)
node_modules/
dist/
.astro/

# OS / editor cruft
.DS_Store
.claude/

# Business/CRM data — must never reach the public web root via the FTP mirror deploy
Client request/
DHYAI_HubSpot_Import.csv
DHYAI_Pipeline_Tracker.xlsx
DHYAI_Prospect_List.xlsx
